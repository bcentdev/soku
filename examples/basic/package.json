{
  "name": "ultra-bundler-benchmark",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build:esbuild": "node esbuild.config.js",
    "build:rspack": "rspack build --config rspack.config.cjs",
    "build:vite": "vite build --outDir ./dist-vite",
    "build:ultra": "../../target/debug/ultra-bundler build --root . --outdir ./dist-ultra"
  },
  "devDependencies": {
    "vite": "^7.1.5",
    "esbuild": "^0.25.10",
    "@rspack/core": "^1.1.11",
    "@rspack/cli": "^1.1.11",
    "css-loader": "^7.1.2"
  }
}